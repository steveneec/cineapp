export const apiBase = "http://localhost:3000";

//Endpoints
export const movies = `${apiBase}/peliculas`;

export const allMovies = `${movies}/all`;

export const movieById = `${apiBase}/peliculas/id`;
